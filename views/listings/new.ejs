<% layout("/layouts/boilerplate") %>
    <style>
        label,
        select {
            display: block;
            width: 100%;
            margin-bottom: 10px;
            /* border: 0.1px solid #F1F2F4 !important; */
        }

        /* Add focus outline style for Select2 */
        .select2-focus {
            border-color: #80bdff !important;
            outline: none !important;
            box-shadow: 0 0 0 0.25rem rgba(0, 123, 255, 0.25) !important;
        }

        /* Remove default border */
        .select2-container--bootstrap .select2-selection--multiple {
            border: none !important;
        }

        /* Style the scrollbar for options dropdown */
        .select2-results__options {
            max-height: 200px;
            /* Set max-height to enable scrollbar */
            overflow-y: auto;
            /* Enable vertical scrollbar */
        }

        /* WebKit (Chrome, Safari) */
        .select2-results__options::-webkit-scrollbar {
            width: 8px;
            /* Set width of the scrollbar */
        }

        .select2-results__options::-webkit-scrollbar-thumb {
            background-color: #ccc;
            /* Color of the scrollbar thumb */
            border-radius: 4px;
            /* Border radius of the scrollbar thumb */
        }

        /* Firefox */
        .select2-results__options {
            scrollbar-width: thin;
            /* Set width of the scrollbar */
            scrollbar-color: #ccc transparent;
            /* Color of the scrollbar thumb and track */
        }
    </style>

    <body>
        <div class="row mt-3">
            <div class="col-8 offset-2">
                <h3>Create a New Listing</h3>
                <form method="POST" action="/listings" class="needs-validation" , novalidate
                    enctype="multipart/form-data">
                    <div class="mb-3">
                        <label for="title" class="form-label">Title</label>
                        <input class="form-control" name="listing[title]" placeholder="Add a catchy title" type="text"
                            required>
                        <div class="valid-feedback">Title looks good!</div>
                        <div class="invalid-feedback">Title should be valid</div>
                    </div>

                    <div class="mb-3">
                        <label for="description" class="form-label">Description</label>
                        <textarea class="form-control" name="listing[description]" placeholder="enter description"
                            required></textarea>
                        <div class="invalid-feedback">Please enter a short description</div>
                    </div>

                    <div class="mb-3">
                        <label for="image" class="form-label">Image</label>
                        <input class="form-control" name="listing[image]" type="file" required>
                        <div class="invalid-feedback">Please enter a valid link for image</div>
                    </div>

                    <!-- <div class="mb-3">
                    <label for="title" class="form-label">Filters</label>
                    <select name="listing[filters]" size="1" class="form-select form-control mb-3" aria-label="Large select example" required>
                        <option selected>Select the options</option>
                        <option value="1">Trending</option>
                        <option value="2">Rooms</option>
                        <option value="3">Iconic Cities</option>
                        <option value="4">Mountains</option>
                        <option value="5">Castles</option>
                        <option value="6">Amazing Pools</option>
                        <option value="7">Camping</option>
                        <option value="8">Rooms</option>
                        <option value="9">Farms</option>
                        <option value="10">Arctic</option>
                        <option value="11">Doams</option>
                        <option value="12">Boats</option>
                    </select>
                    <div class="invalid-feedback">Select any one field</div>
                </div> -->

                    <div class="mb-3">
                        <label for="location" class="form-label">Filters</label>
                        <select multiple name="listing[filters]" id="choices" class="form-control" required>
                            <option>Select the options</option>
                            <option value="trending">Trending</option>
                            <option value="rooms">Rooms</option>
                            <option value="iconiccities">Iconic Cities</option>
                            <option value="mountains">Mountains</option>
                            <option value="castles">Castles</option>
                            <option value="amazingpools">Amazing Pools</option>
                            <option value="camping">Camping</option>
                            <option value="farms">Farms</option>
                            <option value="arctic">Arctic</option>
                            <option value="doams">Doams</option>
                            <option value="boats">Boats</option>
                        </select>
                        <div class="invalid-feedback">Select any one field</div>
                    </div>

                    <div class="row">
                        <div class="mb-3 col-md-4">
                            <label for="price" class="form-label">Price</label>
                            <input class="form-control" name="listing[price]" placeholder="1200" required>
                            <div class="invalid-feedback">Price should be valid</div>
                        </div>

                        <div class="mb-3 col-md-8">
                            <label for="country" class="form-label">Country</label>
                            <input class="form-control" name="listing[country]" placeholder="India" type="text"
                                required>
                            <div class="invalid-feedback">Country should be valid</div>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label for="location" class="form-label">Location</label>
                        <input class="form-control" name="listing[location]" placeholder="Jaipur, Rajasthan" type="text"
                            required>
                        <div class="invalid-feedback">Location should be valid</div>
                    </div>

                    <button class="btn btn-dark add-btn my-3">Add</button>
                </form>
            </div>
        </div>

        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/js/bootstrap.bundle.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.13/js/select2.min.js"></script>
        <script>
            $(document).ready(function () {
                $('#choices').select2({
                    placeholder: 'Select options',
                    allowClear: true,
                    closeOnSelect: false // Keeps the dropdown open after selection
                });

                // Add class on focus
                $('#choices').on('select2:open', function (e) {
                    $(this).siblings('.select2-container').find('.select2-selection--multiple').addClass('select2-focus');
                });

                // Remove class on blur
                $('#choices').on('select2:close', function (e) {
                    $(this).siblings('.select2-container').find('.select2-selection--multiple').removeClass('select2-focus');
                });
            });
        </script>
    </body>